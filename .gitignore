# Rust
target/
Cargo.lock.orig

# Anchor / Solana
.anchor/
test-ledger/
**/*.so

# Keys & secrets — never committed, in any form
*.json.key
id.json
keypair*.json
.env
.env.*

# Node (Phase 6 TS e2e harness)
node_modules/

# Local design/review working notes — scratch material, not project docs.
# Anything meant to be part of the record belongs in docs/adr/ instead.
docs/reviews/

# Claude Code local settings: machine/session-specific, and carries local
# absolute paths and session metadata. Only this one file — the rest of
# .claude/ stays trackable, since project-level agent/skill definitions are
# shared configuration worth committing.
.claude/settings.local.json

# Editors / OS
.DS_Store
.idea/
.vscode/
